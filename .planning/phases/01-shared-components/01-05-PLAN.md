---
phase: 01-shared-components
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Navigation renders with all links working"
    - "Footer renders with all links working"
    - "Theme toggle switches between light/dark"
    - "Scroll animations trigger when scrolling down the page"
    - "Hover states are smooth and responsive"
  artifacts: []
  key_links: []
---

<objective>
Verify all shared components work correctly in the browser

Purpose: Human verification that the foundation is solid before building pages on top of it. This catches visual bugs, animation jank, or broken interactions that automated tests might miss.

Output: Confirmation that Phase 1 foundations are ready for Phase 2+
</objective>

<execution_context>
@C:\Users\Chumm\.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and create test page</name>
  <files>frontend/src/app/(public)/test-components/page.tsx</files>
  <action>
Create a temporary test page that demonstrates all shared components:

```tsx
import { FadeIn } from "@/atoms"
import { Button } from "@/atoms/button"

export default function TestComponentsPage() {
  return (
    <div className="min-h-screen bg-linear-gradient py-20">
      {/* Hero section to test typography */}
      <section className="mx-auto max-w-4xl px-6 mb-20">
        <FadeIn animation="fade-in-up">
          <h1 className="text-hero text-linear-primary mb-6">
            Linear-Style Components
          </h1>
          <p className="text-body-lg text-linear-secondary mb-8">
            Testing the shared component foundation for marketing pages.
          </p>
        </FadeIn>
      </section>

      {/* Button variants */}
      <section className="mx-auto max-w-4xl px-6 mb-20">
        <FadeIn animation="fade-in-up" delay={100}>
          <h2 className="text-headline text-linear-primary mb-8">
            Button Variants
          </h2>
          <div className="flex flex-wrap gap-4">
            <Button variant="linear-primary" size="linear">
              Primary CTA
            </Button>
            <Button variant="linear-secondary" size="linear">
              Secondary
            </Button>
            <Button variant="linear-ghost" size="linear">
              Ghost Link
            </Button>
          </div>
        </FadeIn>
      </section>

      {/* Card hover test */}
      <section className="mx-auto max-w-4xl px-6 mb-20">
        <FadeIn animation="fade-in-up" delay={200}>
          <h2 className="text-headline text-linear-primary mb-8">
            Card Hover Effects
          </h2>
          <div className="grid md:grid-cols-3 gap-6">
            {[1, 2, 3].map((i) => (
              <div
                key={i}
                className="p-6 rounded-lg border border-linear bg-linear-elevated hover-card"
              >
                <h3 className="text-title text-linear-primary mb-2">
                  Card {i}
                </h3>
                <p className="text-meta">
                  Hover to see the card effect with border and shadow transition.
                </p>
              </div>
            ))}
          </div>
        </FadeIn>
      </section>

      {/* Scroll animation test - multiple sections */}
      <section className="mx-auto max-w-4xl px-6 mb-20">
        <FadeIn animation="fade-in-up" delay={300}>
          <h2 className="text-headline text-linear-primary mb-8">
            Scroll Animations
          </h2>
        </FadeIn>
        {[1, 2, 3, 4].map((i) => (
          <FadeIn key={i} animation="fade-in-up" delay={i * 100}>
            <div className="p-6 mb-4 rounded-lg border border-linear bg-linear-elevated">
              <p className="text-body text-linear-secondary">
                Section {i} - Scroll down to see this animate in.
                The animation should be smooth and staggered.
              </p>
            </div>
          </FadeIn>
        ))}
      </section>

      {/* Typography scale */}
      <section className="mx-auto max-w-4xl px-6 mb-20">
        <FadeIn animation="fade-in-up">
          <h2 className="text-headline text-linear-primary mb-8">
            Typography Scale
          </h2>
          <div className="space-y-4">
            <p className="text-hero text-linear-primary">.text-hero (56px)</p>
            <p className="text-display text-linear-primary">.text-display (40px)</p>
            <p className="text-headline text-linear-primary">.text-headline (32px)</p>
            <p className="text-title text-linear-primary">.text-title (20px)</p>
            <p className="text-body-lg text-linear-secondary">.text-body-lg (18px)</p>
            <p className="text-body text-linear-secondary">.text-body (16px)</p>
            <p className="text-meta">.text-meta (13px, muted)</p>
          </div>
        </FadeIn>
      </section>

      {/* Link hover tests */}
      <section className="mx-auto max-w-4xl px-6 pb-20">
        <FadeIn animation="fade-in-up">
          <h2 className="text-headline text-linear-primary mb-8">
            Link Hover Effects
          </h2>
          <div className="flex gap-6">
            <a href="#" className="text-linear-muted hover:text-linear-primary hover-color">
              Color transition
            </a>
            <a href="#" className="text-linear-muted hover-underline">
              Underline animation
            </a>
          </div>
        </FadeIn>
      </section>
    </div>
  )
}
```

This page tests:
- Typography scale (hero through meta)
- Button variants (linear-primary, linear-secondary, linear-ghost)
- Card hover effects
- FadeIn scroll animations with stagger
- Link hover effects
- Dark theme gradient background
  </action>
  <verify>File exists at frontend/src/app/(public)/test-components/page.tsx</verify>
  <done>Test page created demonstrating all shared components</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Linear-style shared components foundation:
- Dark theme utilities (gradients, glows, borders)
- Typography system (hero, display, headline, title, body, meta)
- Scroll animations (FadeIn component)
- Hover interactions (lift, glow, scale, underline)
- Refined navigation and footer
- Linear button variants
  </what-built>
  <how-to-verify>
1. Start the dev server:
   ```bash
   cd frontend && bun run dev
   ```

2. Visit http://localhost:3000/test-components

3. Verify each section:
   - [ ] Typography: All sizes visible with proper hierarchy
   - [ ] Buttons: Hover shows lift effect on primary, color change on secondary/ghost
   - [ ] Cards: Hover shows border brightening and shadow
   - [ ] Scroll: Scroll down slowly - sections should fade in smoothly
   - [ ] Links: Color transition and underline animation work

4. Visit http://localhost:3000 (homepage)
   - [ ] Navigation: All links work, hover states smooth
   - [ ] Theme toggle: Switches between light/dark
   - [ ] Footer: Links work, proper layout
   - [ ] Mobile menu: Works on small screens

5. Check for any console errors in browser DevTools
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Clean up test page</name>
  <files>frontend/src/app/(public)/test-components/page.tsx</files>
  <action>
After verification is approved, delete the test page:

```bash
rm frontend/src/app/(public)/test-components/page.tsx
rmdir frontend/src/app/(public)/test-components
```

The test page was only needed for verification and shouldn't be part of the production codebase.
  </action>
  <verify>The test-components directory no longer exists</verify>
  <done>Test page removed after successful verification</done>
</task>

</tasks>

<verification>
Human verification confirms:
1. All shared component CSS loads without errors
2. Typography scale is visually correct
3. Animations are smooth (no jank)
4. Hover states work on all interactive elements
5. Navigation and footer function correctly
</verification>

<success_criteria>
- User confirms "approved" after visual inspection
- No console errors in browser
- All interactive states work as expected
- Test page cleaned up after verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-components/01-05-SUMMARY.md`
</output>
