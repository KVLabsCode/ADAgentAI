---
phase: 01-shared-components
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - frontend/src/components/organisms/marketing-nav.tsx
  - frontend/src/components/organisms/marketing-footer.tsx
  - frontend/src/components/organisms/index.ts
  - frontend/src/app/(public)/layout-client.tsx
autonomous: true

must_haves:
  truths:
    - "Navigation has refined hover states matching Linear's 100ms transitions"
    - "Footer has refined layout with proper typography classes"
    - "Components use Linear theme utilities (gradients, borders, glows)"
    - "Nav and footer are extracted as reusable organism components"
  artifacts:
    - path: "frontend/src/components/organisms/marketing-nav.tsx"
      provides: "Linear-style navigation bar component"
      exports: ["MarketingNav"]
    - path: "frontend/src/components/organisms/marketing-footer.tsx"
      provides: "Linear-style footer component"
      exports: ["MarketingFooter"]
  key_links:
    - from: "frontend/src/app/(public)/layout-client.tsx"
      to: "marketing-nav.tsx, marketing-footer.tsx"
      via: "component import and prop passing"
      pattern: "import.*Marketing(Nav|Footer)"
    - from: "frontend/src/app/(public)/layout-client.tsx"
      to: "MarketingNav props"
      via: "prop wiring"
      pattern: "user=.*isAuthenticated=.*onSignOut="
---

<objective>
Extract and refine navigation and footer into polished organism components

Purpose: The nav/footer already exist inline in layout-client.tsx but need to be extracted as reusable components with refined styling using the Linear theme utilities created in Plan 01. This enables consistent use across all marketing pages.

Output: MarketingNav and MarketingFooter components with enhanced hover states and typography
</objective>

<execution_context>
@C:\Users\Chumm\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chumm\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/app/(public)/layout-client.tsx
@frontend/src/components/organisms/index.ts
@frontend/src/styles/linear-theme.css
@frontend/src/styles/linear-typography.css
@linear_pics/Screenshot 2026-02-05 054951.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MarketingNav component</name>
  <files>frontend/src/components/organisms/marketing-nav.tsx</files>
  <action>
Extract navigation from layout-client.tsx into a standalone component with enhancements:

1. Create MarketingNav component that receives user/auth state as props:
   ```tsx
   interface MarketingNavProps {
     user: { name: string; email: string; avatar?: string } | null
     isAuthenticated: boolean
     isLoading: boolean
     onSignOut: () => void
     theme: string | undefined
     onThemeToggle: () => void
   }
   ```

2. Apply Linear theme refinements:
   - Use `border-linear` class for bottom border (from linear-theme.css)
   - Add subtle glow effect on CTA button hover using `glow-accent` class
   - Ensure all hover transitions are `duration-100` (Linear's speed)

3. Typography refinements:
   - Nav links: 13.5px, medium weight, `text-linear-muted` -> `text-linear-primary` on hover
   - CTA button text: Use Inter font, medium weight

4. Preserve all existing functionality:
   - Logo link to /
   - Desktop nav links (Platforms, Pricing, Blog)
   - Theme toggle (sun/moon)
   - User dropdown when authenticated
   - Mobile menu dropdown
   - Sign in button

5. Add hover micro-interactions:
   - Nav links: slight scale on hover (1.02) using transform
   - CTA button: subtle shadow lift on hover

Keep the component client-side ("use client") since it uses hooks for auth state.
  </action>
  <verify>
- File exists at frontend/src/components/organisms/marketing-nav.tsx
- grep -E "border-linear|glow-accent|text-linear" frontend/src/components/organisms/marketing-nav.tsx returns matches
  </verify>
  <done>MarketingNav component created with refined Linear styling and hover states</done>
</task>

<task type="auto">
  <name>Task 2: Create MarketingFooter component</name>
  <files>frontend/src/components/organisms/marketing-footer.tsx</files>
  <action>
Extract footer from layout-client.tsx into a standalone component with enhancements:

1. Create MarketingFooter as a simple component (no props needed):
   ```tsx
   export function MarketingFooter() { ... }
   ```

2. Apply Linear theme refinements:
   - Top border: Use `border-linear` class
   - Section headers: Use `text-meta` typography class with `text-linear-primary`
   - Links: Use `text-meta` with `text-linear-muted` -> `text-linear-secondary` hover

3. Layout improvements (reference Linear screenshot):
   - 5-column grid on desktop (logo + 4 link groups)
   - 2-column grid on mobile
   - Proper spacing: py-12 container, mb-4 section headers, space-y-3 links

4. Link groups (matching existing):
   - Product: Platforms, Pricing, Changelog
   - Company: Blog, About
   - Resources: Documentation, Support
   - Legal: Privacy, Terms

5. Add copyright text at bottom:
   ```tsx
   <div className="mt-12 pt-8 border-t border-linear">
     <p className="text-meta text-linear-muted">
       © {new Date().getFullYear()} ADAgent AI. All rights reserved.
     </p>
   </div>
   ```

6. Hover states on all links: 100ms transition, color shift only (no transform)
  </action>
  <verify>File exists with MarketingFooter exported, includes copyright section</verify>
  <done>MarketingFooter component created with refined typography and link organization</done>
</task>

<task type="auto">
  <name>Task 3: Add exports to organisms index</name>
  <files>frontend/src/components/organisms/index.ts</files>
  <action>
Update organisms/index.ts to export the new components:

1. Add export for MarketingNav:
   ```tsx
   export { MarketingNav } from "./marketing-nav"
   ```

2. Add export for MarketingFooter:
   ```tsx
   export { MarketingFooter } from "./marketing-footer"
   ```

3. Keep all existing exports in the file unchanged.
  </action>
  <verify>grep -c "MarketingNav\|MarketingFooter" frontend/src/components/organisms/index.ts returns 2</verify>
  <done>Both MarketingNav and MarketingFooter exported from organisms index</done>
</task>

<task type="auto">
  <name>Task 4: Refactor layout-client.tsx to use extracted components</name>
  <files>frontend/src/app/(public)/layout-client.tsx</files>
  <action>
Update layout-client.tsx to use the new extracted components:

1. Import MarketingNav and MarketingFooter from "@/organisms":
   ```tsx
   import { MarketingNav, MarketingFooter } from "@/organisms"
   ```

2. Replace inline header/nav JSX with MarketingNav, passing all required props:
   ```tsx
   <MarketingNav
     user={user}
     isAuthenticated={isAuthenticated}
     isLoading={isLoading}
     onSignOut={handleSignOut}
     theme={theme}
     onThemeToggle={handleThemeToggle}
   />
   ```

3. Replace inline footer JSX with:
   ```tsx
   <MarketingFooter />
   ```

4. Keep the outer div structure, demo banner logic, and all hook calls (useUser, useTheme, etc.)

The layout-client.tsx should become much shorter (under 80 lines) as the nav/footer are now separate components.

5. Ensure all existing functionality works:
   - Theme toggle
   - User dropdown
   - Mobile menu
   - All links navigate correctly
  </action>
  <verify>
- grep "MarketingNav" frontend/src/app/(public)/layout-client.tsx
- grep "MarketingFooter" frontend/src/app/(public)/layout-client.tsx
- grep -E "user=.*isAuthenticated=.*onSignOut=" frontend/src/app/(public)/layout-client.tsx confirms prop wiring
  </verify>
  <done>layout-client.tsx refactored to use extracted MarketingNav and MarketingFooter with correct prop wiring</done>
</task>

</tasks>

<verification>
1. MarketingNav and MarketingFooter exist as separate files
2. Both are exported from organisms/index.ts
3. layout-client.tsx imports and uses both components with correct props
4. No TypeScript errors on build
5. Navigation and footer render correctly when viewing any public page
</verification>

<success_criteria>
- MarketingNav component exported with all nav functionality preserved
- MarketingFooter component exported with copyright section added
- layout-client.tsx simplified to use extracted components with correct prop wiring
- All hover states have 100ms transitions
- Linear theme utilities (border-linear, text-linear-*) are used
- No build errors, all links work
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-components/01-03-SUMMARY.md`
</output>
