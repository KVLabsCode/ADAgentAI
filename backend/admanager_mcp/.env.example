# Google Ad Manager MCP Server Configuration
# Copy this file to .env and fill in your values

# Option 1: OAuth Access Token (for testing)
# AD_MANAGER_ACCESS_TOKEN=your-oauth-access-token

# Option 2: Service Account (recommended for production)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Server Configuration
# TRANSPORT=stdio  # or "http" for remote access
# PORT=8001        # only used when TRANSPORT=http
